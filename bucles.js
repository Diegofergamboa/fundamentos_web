

/* Los lenguajes de programacion me permiten a través de palabras clave aplicar una tarea a una enorme cantidad de datos */

// Iteración o bucle 

let saludo = 50;

while(saludo > 0) {
    console.log(saludo)
    console.log("hello world");
    saludo = saludo -1 ;
}

/* Otro ejemplo de conteo */

let conteo = 0;

while (conteo < 50) {
    console.log(conteo);
    conteo = conteo + 1;
}
